#include "philo.h"

void	start_dinner(t_table *table)
{
	int	index;

	index = -1;
	if (table->number_of_meals == 0)
		return ;
	else if (table->num_of_philos == 1)
		; //create func
	
	while (++index)
	{
		
	}
}